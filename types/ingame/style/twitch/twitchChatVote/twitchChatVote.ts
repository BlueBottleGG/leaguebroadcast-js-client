/**
 * This is an auto-generated file.
 * Any changes made to this file can be lost when this file is regenerated.
 */

import { globalPosition } from "../../../../shared/style/globalPosition";
import { headline } from "./headline";
import { optionBar } from "./optionBar";
import { optionTitel } from "./optionTitel";
import { optionVotes } from "./optionVotes";
import { totalBadge } from "./totalBadge";
import { voteWrapper } from "./voteWrapper";

export class twitchChatVote {
    position: globalPosition = {} as globalPosition;
    voteWrapper: voteWrapper = {} as voteWrapper;
    headline: headline = {} as headline;
    totalBadge: totalBadge = {} as totalBadge;
    optionTitel: optionTitel = {} as optionTitel;
    optionVotes: optionVotes = {} as optionVotes;
    optionBar: optionBar = {} as optionBar;
    fileVersion: string = "1.0";
}
